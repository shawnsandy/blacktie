<bt-icon-bytes>
		<div data-is="bt-icon"
		src={ opts.src || '/icons/bytesize-symbols.min.svg' }
		icon={ icon || 'i-search' }
		icon-class={ opts.iconClass || 'bt-icon-md'}>
		</div>
</bt-icon-bytes>
